<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"><%= t('application_title') %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/tasks"><%= t('task.tasks').capitalize %> <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= t('language') %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to "中文版", {:controller => 'tasks', :action => 'index', :locale => "zh-TW"}, {:class => "dropdown-item"} %>
          <%= link_to "English", {:controller => 'tasks', :action => 'index', :locale => "en"}, {:class => "dropdown-item"} %>
        </div>
      </li> 
    </ul>
    <ul class="navbar-nav ml-auto">
      <% if @current_user.nil? %>
        <li class="nav-item">
          <a class="nav-link" href="/login"><%= t('user.login').capitalize %></a>
        </li>
      <% else %>  
        <li class="nav-item">
          <%= link_to t('user.logout'), logout_path, method: 'delete', class: "nav-link" %>
        </li>
      <% end %>  
    </ul>  
  </div>
</nav>